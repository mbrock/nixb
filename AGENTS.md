src/tty/example.cpp is the main entry point we're working on right now.

Generally this project is a Nix build monitor.

A working and quite cool implementation is in src/old.

We're trying to rewrite it with better code, using coroutines and stuff.

There's also some P2300 executor stuff that we're experimenting with.

Build with `make build`.  Let me handle reconfiguring CMake when that's necessary,
unless I've explicitly told you to do it.

Prefer to stop and ask me if you get stuck on something.
